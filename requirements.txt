python-dotenv>=1.0
PyYAML>=6.0
requests>=2.31
# GarminDB is used via its CLI (garmindb_cli.py). Install it separately:
#   pip install garmindb
# It is intentionally not pinned here so the pipeline can run against an
# existing GarminDB installation without version conflicts.

# Web dashboard (webapp/) — no login (Tailscale tailnet-only reachability
# is the access control), so no argon2/jinja2/python-multipart needed.
fastapi>=0.115
uvicorn[standard]>=0.32
itsdangerous>=2.2   # signs the CSRF cookie
httpx>=0.27          # fastapi.testclient
pytest>=8.0
